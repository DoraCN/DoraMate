# 25 - 本地运行门禁与稳定性周报 - 2026-04-24

> 周报范围：围绕本周四项工作收口本地运行门禁、样例 smoke、状态一致性与趋势汇总，形成可继续复用的工程入口与留档结果。

## 1. 本周完成情况

### 1.1 CI PR 门禁化

本周已完成代码侧与文档侧接入：

- 新增 PR 轻量门禁 workflow：`.github/workflows/local-runtime-pr-gate.yml`
- 将 PR 门禁拆分为 3 个独立 checks：
  - `windows-pr-localagent-tests`
  - `windows-pr-frontend-tests`
  - `windows-pr-live-smoke`
- 保留现有标准手动门禁：`.github/workflows/local-runtime-standard-release-gate.yml`
- 已在 `docs/22-本地运行发布门禁.md` 中补充 required checks 说明

当前状态：

- 仓库内实现已完成
- GitHub branch protection / ruleset 的实际 required checks 配置仍待管理员执行

### 1.2 多 dataflow 样例 smoke

本周已完成多样例 smoke 的统一入口与首批样例收口：

- 新增脚本：`scripts/smoke-localagent-multi-dataflow.ps1`
- 新增手动 workflow：`.github/workflows/local-runtime-multi-dataflow-smoke.yml`
- 建立两档样例集：
  - `baseline`
  - `standard`

当前 `baseline` 首批样例为：

- `csharp-dataflow/smoke.dataflow.yml`
- `csharp-multi-node/dataflow.yml`
- `csharp-async-node-dataflow/dataflow.yml`

本周同时修复了两类真实问题：

- 多样例预构建阶段的 `DOTNET_CLI_HOME` 环境约束
- 多样例执行过程中 LocalAgent 重启竞争导致的串行样例不稳定

### 1.3 HTTP / WebSocket 一致性校验

本周已完成后端状态一致性收口：

- `doramate-localagent` 中新增统一状态序列化 helper
- HTTP `GET /api/status/:process_id` 与 WebSocket `GET /api/status-stream/:process_id` 当前都基于同一份 `DataflowStatusResponse` 快照构建
- 新增自动化测试，覆盖：
  - `unknown / idle`
  - `registered / running`

同时在 `docs/21-本地运行状态接口契约.md` 中补充了当前一致性约束说明。

### 1.4 结果趋势汇总与阈值微调

本周已完成趋势汇总入口与阈值建议留档：

- 新增趋势脚本：`scripts/summarize-local-runtime-trends.ps1`
- 新增手动 workflow：`.github/workflows/local-runtime-trend-summary.yml`
- 新增阈值建议文档：`docs/24-本地运行门禁趋势汇总与阈值建议.md`

趋势脚本当前会聚合：

- 单样例 smoke
- release gate
- 多样例 smoke

输出位置：

- `out/release-gates/trends/`

## 2. 本周验证结果

本周已完成的关键验证如下：

- `doramate-localagent` 单测通过：`cargo test --locked`
- `baseline` 多样例 smoke 已实际跑通一次，结果为 `3/3 passed`
- 趋势汇总脚本已实际生成 JSON + Markdown 摘要

可直接参考的结果文件：

- `out/release-gates/multi-dataflow/local-runtime-multi-dataflow-smoke-20260424-192401.json`
- `out/release-gates/trends/local-runtime-trend-summary-20260424-193552.json`
- `out/release-gates/trends/local-runtime-trend-summary-20260424-193552.md`

## 3. 当前结论

当前阶段建议结论如下：

1. `run_success_rate`、`status_confirmation_rate`、`stop_success_rate` 继续维持 `100%`
2. `residual_failures <= 0` 继续维持不变
3. `cleanup_failed_count + out_cleanup_failed_count <= 0` 继续维持不变
4. 多样例 smoke 当前先继续积累样本，不建议因为早期失败样本直接放宽门槛

当前判断依据是：

- 最新 smoke 结果为绿色
- 最新标准发布门禁结果为绿色
- 最新 `baseline` 多样例 smoke 结果为绿色
- 现有失败样本主要来自本周已修复的实现问题，而不是已经确认无法消除的长期环境噪声

## 4. 待跟进事项

当前仍有两项后续工作建议继续推进：

1. 请管理员在 GitHub 仓库中把以下 checks 配置为 required：
   - `windows-pr-localagent-tests`
   - `windows-pr-frontend-tests`
   - `windows-pr-live-smoke`
2. 继续补充样本量：
   - `baseline` 多样例 smoke 再累计 `5` 次绿色结果
   - `standard` 多样例 smoke 至少累计 `3` 次绿色结果

如果继续推进下一阶段，优先建议先跑 `standard` 多样例 smoke，再观察是否需要把更多样例纳入长期门禁。
